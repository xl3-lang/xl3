# ADR 0018 - reserved (gap placeholder)

- **Status:** reserved
- **Date:** 2026-05-09
- **Spec target:** —
- **Affects:** —

## Context

ADR-0018 was skipped during the deferral-ADR pair (0019 date
arithmetic, 0020 locale collation) without a corresponding draft.
This file marks the gap so the ADR-numbering audit (`spec-lint`
test) does not flag it as missing and so future readers don't
wonder whether they're looking at an incomplete ADR set.

If a future spec decision is drafted, it MAY take the 0018 number
or the next free one — either is fine; the ADR catalog is
order-independent.
