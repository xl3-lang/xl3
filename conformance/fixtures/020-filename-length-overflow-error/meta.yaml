description: Output filename sanitization reports an error above the 255-byte limit.
spec_section: evaluation.md "Output Filenames"
spec_version: "0.1"
tags: [filename, sanitization, error]
verified_by: [manual-script]
expected_error: "exceeds the 255-byte limit"
