description: The outermost @subtotal row fires once at the end of the data block — the outermost group's boundary IS the end of the data (ADR-0038). Used as the "grand total" pattern without a separate directive.
spec_section: ADR-0038
spec_version: "0.1"
tags: [adr-0038, group, subtotal, grand-total]
verified_by: [hand]
