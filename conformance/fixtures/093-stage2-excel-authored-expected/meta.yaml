description: Synthetic cross-writer Stage 2 — expected.xlsx is the engine's output passed through `conformance/scripts/perturb-xlsx.mjs`, which reorders attributes, flips quote style, and reverses zip entry order. Canonicalizer rules 1 and 3 must normalize all of these.
spec_section: ADR-0006 amendment "canonicalizer scope and gaps"; conformance/runner-protocol.md "Stage 2 output comparison" rules 1, 3
spec_version: "0.1"
tags: [stage2, canonicalization, cross-writer]
comparison_stage: 2
verified_by: [synthetic-perturbation]
