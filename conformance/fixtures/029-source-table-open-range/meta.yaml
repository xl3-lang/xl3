description: source_table open range selects a column window and reads rows below through the used row end.
spec_section: evaluation.md#source-data-model
spec_version: "0.1"
tags: [config, source-table]
verified_by: [manual-script]

