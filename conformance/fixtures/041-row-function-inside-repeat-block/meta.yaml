description: ROW() returns the 1-based index of the current rendered data row inside a repeat block.
spec_section: language.md "Row and Date Functions"
spec_version: "0.1"
tags: [language, row, repeat]
verified_by: [manual-script]
