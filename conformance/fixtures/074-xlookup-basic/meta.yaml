description: 3-arg XLOOKUP returns the matched return-array column for the first row whose lookup-array matches per ADR-0013.
spec_section: language.md "XLOOKUP"; ADR-0013
spec_version: "0.1"
tags: [language, function, xlookup, sources]
verified_by: [hand]
