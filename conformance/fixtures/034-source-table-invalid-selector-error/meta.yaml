description: source_table reports an error for invalid selectors such as row zero.
spec_section: evaluation.md#source-data-model
spec_version: "0.1"
tags: [config, source-table, error]
verified_by: [manual-script]
expected_error: "source_table row numbers must be 1-based positive integers"

expected_error_code: 'xl3/config/invalid-source-table'
