description: COUNT([field]) counts non-empty values in the current row set.
spec_section: language.md "Functions / Aggregates"
spec_version: "0.1"
tags: [language, aggregate, count]
verified_by: [manual-script]
